---
interface props {
    url: string
    name?: string,
}

const { name, url } = Astro.props
---

<li>
    <a href={url} target="_blank" class="flex justify-center">
        {name?? name}
        <slot />
    </a>
</li>