---
import { getLangFromUrl } from '@i18n/utils'

const lang = getLangFromUrl(Astro.url)

interface SeoTags {
	description: string,
	keywords: string,
	robots?: string
}

interface Props {
	title: string;
	seoTags: SeoTags
}

const { title, seoTags } = Astro.props;
---

<!doctype html>
<html lang={lang}>
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Astro description" />
		<meta name="viewport" content="width=device-width" />
		<link rel="shortcut icon" href="" />
		<meta name="generator" content={Astro.generator} />

		<link rel="stylesheet" href="../assets/css/main.css">

		<!-- SEO -->
		<meta name="description" content={seoTags.description}>
		<meta name="keywords" content={seoTags.keywords}>
		<meta name="author" content="Snowmilk Teas">
		<meta name="robots" content={seoTags.robots || "index, follow"}>

		<link rel="canonical" href="TODO parametrizar la url canónica">

		<title>{title}</title>
	</head>
	<body>
		<slot />
	</body>
</html>
<style is:global>
	:root {
		--accent: 136, 58, 234;
		--accent-light: 224, 204, 250;
		--accent-dark: 49, 10, 101;
		--accent-gradient: linear-gradient(
			45deg,
			rgb(var(--accent)),
			rgb(var(--accent-light)) 30%,
			white 60%
		);
	}
	body {
		/* background: rgb(255,255,255);
		background: linear-gradient(180deg, rgba(255,255,255,1) 0%, rgba(255,168,254,1) 52%, rgba(0,212,255,1) 100%);
		background-repeat: no-repeat; */
	}
</style>
